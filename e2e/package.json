{
  "name": "cyrene-admin-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end test suite for CyreneAdmin using Playwright",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "dependencies": {
    "sm-crypto": "^0.3.13"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0",
    "@types/node": "^22.0.0"
  }
}
